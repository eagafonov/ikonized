#!/usr/bin/make -f

export DH_VERBOSE=1

%:
	dh $@ --buildsystem=cmake --builddirectory=obj-build

override_dh_auto_configure:
	dh_auto_configure --builddirectory=obj-build -- -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release
